// Extra flag types for the built-in flag library.
//
// flagutil provides new Functions for adding flags to the
// default command line parser (flag.Parse()) as well as
// to a custom *flag.FlagSet.
package flagutil
